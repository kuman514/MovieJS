class YTLoader {
  constructor(urlInput, loadButton, video) {
    this.urlInput = urlInput;
    this.loadButton = loadButton;
    this.video = video;

    this.loadButton.addEventListener(() => {
      // Parse youtube link first
    });
  }
}